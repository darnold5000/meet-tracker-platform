# Base URL of the FastAPI backend (08 app or deployed Cloud Run Service).
# No trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:8002

# Default meet to show when none is selected (optional).
# NEXT_PUBLIC_DEFAULT_MEET_KEY=MSO-36478
